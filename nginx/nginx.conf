events { worker_connections 1024; }

http {
    server {
        listen 80;

        location / {
            proxy_pass http://c_task_api:8080;
            proxy_set_header Host $http_host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}